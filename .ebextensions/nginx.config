files:
    "/etc/nginx/conf.d/000_new_nginx_config.conf":
    content: |

    server {
        listen 80;

        location / {
            if ($http_x_forwarded_proto != 'https') {
                rewrite ^ https://$host$request_uri? permanent;
            }
        }
    }
